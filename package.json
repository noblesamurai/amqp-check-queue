{
  "name": "amqp-check-queue",
  "description": "Check status of an AMQP queue.",
  "version": "1.0.0-init.0",
  "author": "Tim Allen <tim@noblesamurai.com>",
  "license": "BSD",
  "main": "index.js",
  "scripts": {
    "coverage": "nyc -a -c -r html -r text -r lcov npm test",
    "pretest": "semistandard --env mocha",
    "test": "mocha --recursive test",
    "watch": "mocha --recursive --watch test",
    "docs": "jsdoc2md index.js"
  },
  "homepage": "https://github.com/noblesamurai/amqp-check-queue",
  "repository": {
    "type": "git",
    "url": "git://github.com/noblesamurai/amqp-check-queue.git"
  },
  "bugs": {
    "url": "https://github.com/noblesamurai/amqp-check-queue/issues"
  },
  "engines": {
    "node": "8.x",
    "npm": "5.x"
  },
  "dependencies": {},
  "devDependencies": {
    "chai": "^3.5.0",
    "dirty-chai": "^1.2.2",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "semistandard": "*",
    "jsdoc-to-markdown": "^4.0.0"
  },
  "keywords": [],
  "nyc": {
    "exclude": [
      "coverage",
      "test"
    ]
  },
  "semistandard": {
    "env": [
      "mocha"
    ]
  }
}
